# Ignore files marked "-local"
**/*-local

# Claude Code plan files (ephemeral, not meant to sync across machines)
.claude/plans

# Machine-local Claude settings (not meant to sync across machines)
.shell_modules/.claude/settings.local.json